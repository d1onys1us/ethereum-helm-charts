apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "taiko-geth.fullname" . }}-test-connection"
  labels:
    {{- include "taiko-geth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "taiko-geth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
