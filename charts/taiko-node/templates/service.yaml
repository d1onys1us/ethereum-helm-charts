apiVersion: v1
kind: Service
metadata:
  name: {{ include "taiko-node.fullname" . }}
  labels:
    {{- include "taiko-node.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taiko-node.selectorLabels" . | nindent 4 }}
