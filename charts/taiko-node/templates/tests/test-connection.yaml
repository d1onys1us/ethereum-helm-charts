apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "taiko-node.fullname" . }}-test-connection"
  labels:
    {{- include "taiko-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "taiko-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
